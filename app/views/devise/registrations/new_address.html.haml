= render "products/header"
.sell__wrapper
  .sell__header
  .regist__container
    .sell__container__images
      .user__container__images__titles
        商品送付先情報入力

    .entrySell__container__description
      .sell__container__description__address
        =form_for(@address, url: addresses_path) do |f|
          =render "devise/shared/error_messages", resource: @address
          .label
            .label__content
              商品送付先氏名(全角)
            .label__required
              必須
          .entryform__parallel
            = f.text_field :family_name, autofocus: true, autocomplete: "family_name", placeholder: "例）振馬", class: 'input-box'
            = f.text_field :first_name, autofocus: true, autocomplete: "first_name", placeholder: "例）太郎", class: 'input-box'
          .label
            .label__content
              商品送付先氏名カナ(全角)
            .label__required
              必須
          .entryform__parallel
            = f.text_field :family_name_kana, autofocus: true, autocomplete: "family_name_kana", placeholder: "例）フリマ", class: 'input-box'
            = f.text_field :first_name_kana, autofocus: true, autocomplete: "first_name_kana", placeholder: "例）タロウ", class: 'input-box'
          .label
            .label__content
              郵便番号
            .label__required
              必須
          .entryform__parallel
            = f.text_field :postalcode, autofocus: true, autocomplete: "postalcode", placeholder: "例）123-456", class: 'input-box'
          .label
            .label__content
              都道府県
            .label__required
              必須
          .entryform__parallel
            = f.text_field :prefecture, autofocus: true, autocomplete: "prefecture", placeholder: "例)大阪府", class: 'input-box'
          .label
            .label__content
              市町村
            .label__required
              必須
          .entryform__parallel
            = f.text_field :city, atuofocus: true, atuocomplete: "city", placeholder: "例)大阪市中央区", class: 'input-box'
          .label
            .label__content
              番地
            .label__required
              必須
          .entryform__parallel
            = f.text_field :address, atuofocus: true, atuocomplete: "address", placeholder: "例)難波5丁目1-60", class: 'input-box'
          .label
            .label__content
              建物名
            .label__any
              任意
          .entryform__parallel
            = f.text_field :buildingname, atuofocus: true, atuocomplete: "buildignname", placeholder: "例)なんばスカイオ 15階", class: 'input-box'
          .label
            .label__content
              電話
            .label__any
              任意
          .entryform__parallel
            = f.text_field :phone, atuofocus: true, atuocomplete: "phone", placeholder: "例)09012345678", class: 'input-box'
          .cautionMess__gray
            ※本人情報は正しく入力してください。登録後、修正するにはお時間をいただく場合がございます。
          .sell__container__btn
            .sell__container__btn__sell
              = f.submit "登録する", class: 'regist-btn'
            .sell__container__btn__save
              = f.submit "下書きに保存", class: 'temporary__regist-btn'
            .sell__container__btn__return
              = link_to "もどる", new_user_registration_path
= render "products/footer"